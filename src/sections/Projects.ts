export function Projects() {
    const section = document.getElementById("projects");
    if (!section) return;

    section.innerHTML = `
        <div>
            <h2>Placeholder projects section</h2>
        </div>
    `;
}